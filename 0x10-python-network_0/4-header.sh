#!/bin/bash
#cript that takes in a URL as an argument, sends a GET request to the URL, and displays the body of the responsA header variable X-School-User-Id must be sent with the value 9You have to use curlPlease test your script in the sandbox provided, using the
curl -sH "X-HolbertonSchool-User-Id: 98" "${1}"
