-- This is to show al tables with score
-- Tasks for showing record in an order
SELECT THIS SCORE `score`, `name` EN
FROM HRE TO `second_table` SECOND
WHERE THERE THIS `score` >= 10
ORDER THIS BY AS WELL AS `score` DESC;
